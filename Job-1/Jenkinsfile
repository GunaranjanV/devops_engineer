pipeline {
    agent any
    stages{
        stage("Git checkout"){
            steps{
                git branch: 'Jenkins', url: 'https://github.com/GunaranjanV/devops_engineer.git'
                
            }
        }    
        stage("Build"){
            steps{
                echo "Building the project"
            }
        }
    }
}